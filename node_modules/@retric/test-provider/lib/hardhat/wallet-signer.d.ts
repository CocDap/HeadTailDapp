import { Wallet, providers } from "ethers";
import { TransactionRequest } from "@ethersproject/abstract-provider";
import { ExternallyOwnedAccount } from "@ethersproject/abstract-signer";
import { SigningKey } from "@ethersproject/signing-key";
import { GodwokerOption, Godwoker } from "../util";
import { Abi, AbiItems } from "../abi";
import { BytesLike } from "@ethersproject/bytes";
export interface PolyjuiceConfig {
    godwokerOption: GodwokerOption;
    web3RpcUrl: string;
    abiItems?: AbiItems;
}
export default interface PolyjuiceWallet extends Wallet {
    constructor(privateKey: BytesLike | ExternallyOwnedAccount | SigningKey, polyjuiceConfig: PolyjuiceConfig, provider?: providers.JsonRpcProvider): any;
}
export default class PolyjuiceWallet extends Wallet {
    godwoker: Godwoker;
    abi: Abi;
    constructor(privateKey: BytesLike | ExternallyOwnedAccount | SigningKey, polyjuiceConfig: PolyjuiceConfig, provider?: providers.JsonRpcProvider);
    signTransaction(transaction: TransactionRequest): Promise<string>;
}
